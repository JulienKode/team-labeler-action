name: "build"
on:
  pull_request:
  push:
    branches:
      - master
      - 'releases/*'

jobs:
  build:
    runs-on:
      - self-hosted
      - 1ES.Pool=MC-Runner-1ES-Windows
    steps:
    - uses: actions/checkout@v3
    - run: |
        yarn
        yarn all
